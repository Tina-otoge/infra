services:
  jellyfin:
      image: jellyfin/jellyfin
      ports:
        - "{{ loopback_ip }}:{{ jellyfin_port }}:{{ jellyfin_port }}"
      volumes:
        - "{{ ansible_facts.user_dir }}/mnt/freebox:/mnt/Freebox"
        - "{{ ansible_facts.user_dir }}/mnt/sakura:/mnt/sakura/public"
        - "{{ ansible_facts.user_dir }}/.config/jellyfin:/config"
        - "{{ ansible_facts.user_dir }}/.cache/jellyfin:/cache"
      network: six
      restart: unless-stopped

networks:
  six:
